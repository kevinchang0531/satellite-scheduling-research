# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 專案狀態

`ClaudeEP04` 是一個測試專案,目前僅有骨架(`.gitignore`、`README.md`、本檔案),尚未加入實際程式碼、技術棧或建置系統。

## 給後續 Claude 的指引

- 尚未選定語言/框架。實際導入程式碼後,請重新執行 `/init` 以分析真實結構,並在此補上建置、lint、測試(含單一測試)指令與架構說明——目前這些資訊都還不存在,不要憑空編寫。
- 與使用者(Kevin)溝通請用繁體中文。
